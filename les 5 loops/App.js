class App
{
    runApplication()
    {
        console.log("Hello world!");
        Array = ["Maroon 5", "Alec Benjamin", "Train", "AC/DC", "Ninja Sex Party"];
    }
    tekencircle(x,y)
    {
        for(this.tekencircle; 200;);
        g.beginPath();
        g.fillStyle = "red";
        g.moveTo(x,y);
        g.arc(x,y,20,0,Math.PI*2);
        g.stroke();
        g.fill();
        g.closePath();
    }
}
let app = new App();
app.runApplication(); 